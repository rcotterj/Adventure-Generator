def parseUserInput(userInput):
    tokenizedInput = userInput.split(" ")
    return tokenizedInput

#look, take, move
def takeAction(action):
    if action[0].ascii_lowercase == 'look':
        
    if action[0].ascii_lowercase == 'take':

    if action[0].ascii_lowercase == 'move':

    else:
        return(0);
